 window.addEventListener('click', event => {clickFunction(event);});

function clickFunction(e){
     console.log(e.target.value)
 } ;




//step 1 - capture radio button values
// save these to variables
//write logic for when all three categories have been selected
//display text after this